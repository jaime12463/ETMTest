import { makeStyles } from "@material-ui/core";
const usarEstilos = makeStyles({
    container: {
        marginTop: 12,
        maxHeight: 300,
    },
    alignment: {
        textAlign: "center",
    },
});
export default usarEstilos;