import { FunctionComponent } from 'react';

type Props = {
};

const InfoPedidoActual: FunctionComponent<Props> = (props) => {

    return (
        <div>
            InfoPedidoActual
        </div>
    );
} 

export default InfoPedidoActual;