import {GlobalStyles as GlobalThemeStyles} from '@mui/material';

const GlobalStyles = () => {
	return (
		<GlobalThemeStyles
			styles={{
				body: {
					background: '#E5E5E5',
				},
			}}
		/>
	);
};

export default GlobalStyles;
