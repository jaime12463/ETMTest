import {createTheme} from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#f33737',
		},
		secondary: {
			main: '#f33737',
		},
		success: {
			main: '#00CF91',
		},
	},
});

export default theme;
