import {makeStyles} from '@material-ui/core/styles';
const useEstilos = makeStyles({
	container: {
		marginTop: 12,
		maxHeight: 150,
	},
	alignment: {
		textAlign: 'center',
	},
});
export default useEstilos;
